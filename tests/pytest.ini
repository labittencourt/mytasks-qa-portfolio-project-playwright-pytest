[pytest]
markers =
    api: API tests (backend, no browser)
    e2e: end-to-end interface tests (frontend, via Playwright)
    wip: work in progress — temporarily mark a test to run it in isolation
    smoke: critical regression subset — core happy paths across auth, todos and session

addopts = -v --browser chromium -n auto
